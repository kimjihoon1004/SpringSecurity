<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eugeneprogram.testweb.dao.TestMapper">
    <select id="getList" resultType="java.util.Map">
        SELECT user_id, user_pw FROM user;
    </select>
    
    <select id="comparePw" resultType="Integer" parameterType="String">
        SELECT COUNT(*) AS pw FROM user WHERE user_pw = PASSWORD(#{pw}) AND user_id = #{id};
    </select>
    
    <select id="getAllList" resultType="java.util.Map">
        SELECT * FROM user;
    </select>
    
    <insert id="insertId" parameterType="java.util.Map">
        INSERT INTO user(user_name, user_id, user_pw, user_phone, user_date) VALUES(#{userName}, #{userId}, password(#{userPw}), #{userPhone}, #{userDate});
    </insert>
    
</mapper>

 